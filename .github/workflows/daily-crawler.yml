name: Daily crawler

on:
  schedule:
    - cron: "30 7 * * 1-5"
  workflow_dispatch:

jobs:
  crawl:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: pip install -r requirements.txt
      - run: python -c "from app.api import AppApi; AppApi(__import__('pathlib').Path('data/market.sqlite')).sync_today()"
      - run: python -m crawlers.twse_otc_quotes
      - uses: actions/upload-artifact@v4
        with:
          name: market-sqlite
          path: data/market.sqlite
